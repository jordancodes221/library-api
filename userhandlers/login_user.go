package userhandlers

// Struct of login JSON payload (username, plain text password)

// Password hash function (input plain text password, output hash of that text)

// Verify (verify if the output of the password hash function above matches the hashed password in the database for that particular username)




